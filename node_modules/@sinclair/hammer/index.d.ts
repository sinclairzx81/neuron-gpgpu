export { folder, file, shell, watch, delay } from './task/global/index';
import { Dispose } from './dispose';
import * as options from './options/index';
declare class Hammer implements Dispose {
    private readonly options;
    private readonly disposables;
    constructor(options: options.Options);
    private build;
    private watch;
    private serve;
    private run;
    private monitor;
    private task;
    private getVersion;
    private version;
    private help;
    execute(): Promise<void>;
    dispose(): void;
}
export declare function hammer(options: options.Options): Hammer;
