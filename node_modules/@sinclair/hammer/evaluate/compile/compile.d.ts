export interface CompileResult {
    filename: string;
    dirname: string;
    code: string;
}
/** Compiles the given code into single executable script. */
export declare function compile(scriptPath: string): CompileResult;
